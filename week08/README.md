# 第 8 週｜函數與單元測試（2026/10/29）

- 對應教科書：Ch6 函數與錯誤處理
- 教學內容：
  - 6-2 自訂函數、6-3 變數範圍、6-6 錯誤處理
  - 【測試主題 4】迷你單元測試：對一個 PHP 函數寫 3 個 assert()，跑過才算完成（不裝 PHPUnit）

## 隨堂作業

作業W08：寫一個計算函數，再用 3 個 assert() 驗證它（截圖 assert 全過）

- 第 3 節上機時完成，貼到當週 Canva 答題卡自己學號末兩碼的卡片，下課前完成

## 作業W08 學習單（1 題）

**題目**：寫一個 PHP 函數 `is_long_stay($hours)`，靠泊 > 20 回傳 true 否則 false。**測試主題 4**：寫 3 個 `assert()` 驗證它——`assert(is_long_stay(25)===true)`、`assert(is_long_stay(15)===false)`、`assert(is_long_stay(20)===false)`，三個都要通過。

**資料**：無（自訂函數）。

**繳交物**：程式碼截圖＋執行畫面（assert 全過、無錯誤訊息）。

**評分要點（Pass／Fail）**：函數邏輯正確；3 個 assert 都寫了且都通過。

## 註解：小考 1（Ch1–Ch7＋SQL）；期中複習

見 [quizzes/README.md](../quizzes/README.md)。
