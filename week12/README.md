# 第 12 週｜網站登入與 SQL 注入防護（2026/11/26）

- 對應教科書：Ch11 應用實例、Ch13 PDO
- 教學內容：
  - 11-5 網站登入實例
  - 13-5 PDO 與 prepared statement
  - 【測試主題 6】SQL 注入試探：在登入表單輸入 `' OR 1=1 --`，證明字串拼接會被打穿，改 PDO prepared statement 後擋住

## 隨堂作業

作業W12：做登入頁，先示範注入成功，再改用 PDO 擋住（兩張截圖）

- 第 3 節上機時完成，貼到當週 Canva 答題卡自己學號末兩碼的卡片，下課前完成

## 作業W12 學習單（1 題）

**題目**：做一個登入頁（帳號密碼查一張 users 表）。**測試主題 6（只在自己電腦的 XAMPP 上做）**：先用字串拼接寫 SQL，在帳號欄輸入 `' OR 1=1 --` 示範被繞過登入；再改用 PDO prepared statement，證明同樣輸入無法登入。

**資料**：自己建的 users 表（2–3 筆假帳號）。

**繳交物**：兩張截圖：注入成功（拼接版）、注入失敗（PDO 版）。

**評分要點（Pass／Fail）**：兩版都展示；能說明 prepared statement 為何擋得住。
