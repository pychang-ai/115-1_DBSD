# 第 15 週｜效能、索引與壓力測試（2026/12/17）

- 對應教科書：Ch12 索引＋專案開發
- 教學內容：
  - 索引 INDEX、EXPLAIN 看查詢計畫
  - 【測試主題 9】壓力測試：用 XAMPP 內建 ApacheBench `ab -n 1000 -c 50` 打自己的查詢頁，看每秒請求數與失敗數；加索引前後用 EXPLAIN 比較
  - 專案開發時間

## 隨堂作業

作業W15：對自己的查詢頁跑 ab 壓測，加索引前後各測一次，記錄請求數／秒

- 第 3 節上機時完成，貼到當週 Canva 答題卡自己學號末兩碼的卡片，下課前完成

## 作業W15 學習單（1 題）

**題目**：對第 10 或 14 週的查詢頁，用 XAMPP 內建 `ab`（`\xampp\apache\bin\ab.exe`）跑 `ab -n 1000 -c 50 http://localhost/你的頁面`。**測試主題 9**：對常用查詢欄位加 `INDEX`，加索引前後各跑一次 `EXPLAIN` 與 `ab`，記錄每秒請求數的變化。

**資料**：自己的 ships 資料表與查詢頁。

**繳交物**：ab 兩次結果截圖（加索引前後）＋一句 Requests/sec 的比較。

**評分要點（Pass／Fail）**：ab 有跑成功（1000 請求）；有加索引並比較前後；EXPLAIN 有附。

## 註解：專案進度檢查

課程照常進行。
